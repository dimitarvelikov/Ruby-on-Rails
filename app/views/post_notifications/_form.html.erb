<%= form_with(model: post_notification, local: true) do |form| %>
  <% if post_notification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_notification.errors.count, "error") %> prohibited this post_notification from being saved:</h2>

      <ul>
      <% post_notification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.number_field :post_id, id: :post_notification_post_id %>
  </div>

  <div class="field">
    <%= form.label :seen %>
    <%= form.check_box :seen, id: :post_notification_seen %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
